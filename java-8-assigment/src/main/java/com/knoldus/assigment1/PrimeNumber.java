/*
package com.knoldus.assigment1;

import java.util.ArrayList;
import java.util.List;
import java.util.Random;
import java.util.function.IntPredicate;
import java.util.stream.Collector;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class PrimeNumber {


   */
/* private static boolean isPrime(int number) {
        IntPredicate isDivisible = index -> number % index == 0;
        return number > 1 && IntStream.range(2, number - 1).noneMatch(isDivisible);
    }*//*


    public static void main(String[] args) {
        int initialSize = 100;
        List<Integer> numbers = new ArrayList<Integer>(initialSize);

        Random randomGenerator = new Random();


        for (int i = 0; i < initialSize; i++) {
            numbers.add(randomGenerator.nextInt(100));
            //System.out.println(numbers);
        }
         isPrime(numbers);

    }

    private static void isPrime(List<Integer> numbers) {

        (numbers) ->{

        }


    }
}
*/
